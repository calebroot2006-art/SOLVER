[package]
name = "bestresponse"
version = "0.1.0"
description = "Exploitability on the public-state tree, reported as a percentage of the pot"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
postflop = { path = "../postflop" }
