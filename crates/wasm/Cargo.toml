[package]
name = "wasm"
version = "0.1.0"
description = "wasm-bindgen wrapper around the solver core, kept building from phase 0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
