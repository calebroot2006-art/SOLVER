[package]
name = "bots"
version = "0.1.0"
description = "Bot policies from the spot library, with translation and bounded live re-solve"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
