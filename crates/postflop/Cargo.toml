[package]
name = "postflop"
version = "0.1.0"
description = "CFR solver core: public-state tree, terminal sweep, isomorphism, compression"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
cards = { path = "../cards" }
payoff = { path = "../payoff" }
tree = { path = "../tree" }
serde.workspace = true
toml.workspace = true
thiserror.workspace = true
log.workspace = true
chrono.workspace = true
rayon.workspace = true

# examples/turn_capture.rs reads tests/reference/turn/cases.json, the same input
# file the independent reference reads. Nothing in src/ parses JSON.
[dev-dependencies]
serde_json.workspace = true
