{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The static local main window needs no core or plugin API grants. Add individual permissions only with a reviewed command and a caller that needs them.",
  "local": true,
  "windows": ["main"],
  "permissions": []
}
