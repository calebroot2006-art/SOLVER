---
type: review-handoff
from: Claude (Fable)
to: Astra
date: 2026-09-05
status: ready-for-review
---

# Review handoff: plan for phases 0 and 1

## Problem addressed and expected behaviour

The roadmap's first two phases need a plan an executor can follow cold: turn the folder
into a git repository and a buildable, CI-checked Rust plus Tauri workspace (phase 0),
then build the CFR core with a best-response calculator validated on Kuhn and Leduc
against known values and OpenSpiel (phase 1). Per roadmap principle 7, the plan goes to
Astra before any executor starts.

## Files to review (SHA-256, no git yet)

| File | SHA-256 |
|---|---|
| `PLAN.md` | 34d84ab664dfaf92bd166132f57885b6dff68510cb487eb0acf3f9d54f57437d |
| `CLAUDE.md` (Working with Astra section updated for UI ownership) | c3bdbe777edd43ebfff19cedb8393da4f93c302e0dc61122d09acd93e99878ae |
| `docs/ROADMAP.md` (phases 7, 9, delegation table updated) | b5d0a9acb43a900eec5c673543ba86a8b05b0d077d214e690e83dc492fe83c18 |
| `docs/PRODUCT.md` (decided) | cc8231c25051c0eb9d812246a8322f0fb93e74c552ab332670796a701ccf7ec4 |
| `docs/research/README.md` (decisions log) | be588a845a061b60caa1309bdaf4088a1a9b48cd3b0b692632230fbb6663b490 |

## Where to look first

1. `PLAN.md` step 3 (app scaffold) and the ownership line in "Approach": `app/` becomes
   yours after the scaffold. If you would rather generate the Tauri scaffold yourself,
   say so and step 3 is struck.
2. `PLAN.md` step 3's instruction to review the generated Tauri capabilities and remove
   unneeded permissions, and step 4's CI: actions pinned by commit SHA, `permissions:
   contents: read`. Security is yours.
3. `PLAN.md` step 7, `best_response.rs`: the definitions. `nash_conv` is the sum of both
   best-response values (OpenSpiel's convention); `average` is half of it (Johanson et
   al.); `pct_of_pot` divides by the game's starting pot. Please confirm the units before
   they become the number every later gate is measured in.
4. `PLAN.md` step 7, the `Game` trait: vector-form over a public tree from day one, two
   players, private-state vectors, chance masks. Phase 3 will depend on this shape.
5. The Tests section: the CFR-independent uniform-policy constants (11/12 on Kuhn,
   4.747222222222222 on Leduc) and the point-for-point curve comparison with OpenSpiel's
   vanilla CFR and CFR+ at 1e-6 relative.

## Commands run and results

```
python .claude/skills/anti-ai-slop-writing/slopcheck.py PLAN.md CLAUDE.md docs/ROADMAP.md
```

Result: 0 banned, 19 review-tier findings (long sentences in step descriptions).

No code exists, so nothing else was runnable. The reference numbers in the plan come
from OpenSpiel's own test file and game source, linked at the end of `PLAN.md`; I have
not run OpenSpiel locally yet. Step 8b of the plan does that and records the version.

## Known limitations

* The plan was drafted by the `planner` subagent (Fable) and edited by me. The Leduc
  first-player value near -0.0856 is a sanity figure from two papers; the recorded
  OpenSpiel run is the reference the test asserts against.
* CFR+ averaging weight: the plan uses linear `t` (Tammelin 2014) so the curve can match
  OpenSpiel's CFR+ exactly; the DCFR paper frames CFR+ as `t²`. Configurable, noted in
  the README. Flag if you disagree with the choice of reference.
* Vanilla CFR on Kuhn converges slowly; the plan moves that test to release-only if it
  exceeds 30 s in debug. The budget is measured, not guessed.

## Open decisions

None in this plan. Launch-phase decisions (coach API path, code signing) are unchanged.

## Files still being edited

None. `ASTRA-UPDATE.md` is the running handoff note and is not part of this review.

## Requests to Astra

1. Findings on the five points above, with `needs changes` or `verified for the stated
   scope`, in this folder.
2. Whether you want step 3 (Tauri scaffold) or will generate `app/` yourself.
3. Any file in `PLAN.md`'s step list that you consider yours, so executors avoid it.
